{
  "_comment": "Answer-bearing key facts per workload — the quality benchmark checks how many survive the optimizer's trim (deterministic substring + optional LLM judge). Most key facts are verbatim substrings of that workload's payload; a few (flagged in the per-entry _note, e.g. 16-test-run) are verbatim substrings of the strategy's reformatted output instead. See run_quality.mjs / lib/quality.mjs.",
  "1-access-log": {
    "question": "Find the failing requests: which endpoint and method are returning 5xx errors (HTTP 500), how many, the time window, and the client IPs.",
    "keyFacts": [
      "POST /api/checkout HTTP/1.1\" 500",
      "/api/checkout",
      "10:24:30",
      "10:29:10",
      "203.0.113.21"
    ]
  },
  "2-sre-incident": {
    "question": "Checkout p99 latency spiked around 10:05 and we got paged. Find what caused the spike.",
    "keyFacts": [
      "failed to acquire db connection",
      "pool=primary",
      "max_conns=20 in_use=20",
      "10:05:00.100Z",
      "wait_ms=3500"
    ]
  },
  "4-json-array": {
    "question": "How many orders failed, and what failure_code does each failed order show? Ignore the paid ones.",
    "keyFacts": [
      "\"status\": \"failed\"",
      "ord_1N0000rt",
      "\"failure_code\": \"card_declined\"",
      "\"failure_code\": \"expired_card\"",
      "\"failure_code\": \"insufficient_funds\"",
      "\"failure_code\": \"do_not_honor\""
    ]
  },
  "5-code-search": {
    "question": "Where is the retry policy / max-attempts actually defined (not just used)? Point me to the file and line.",
    "keyFacts": [
      "src/lib/http/retryPolicy.ts",
      "RETRY_MAX = 5",
      "max retry attempts for idempotent HTTP requests"
    ]
  },
  "6-git-diff": {
    "question": "Is there any change in this PR diff that weakens an auth or permission check (a role or admin check that got loosened)? Point to the exact file.",
    "keyFacts": [
      "src/auth/middleware.ts",
      "requireAdmin",
      "user.role === \"admin\"",
      "if (user && user.isAuthenticated) { // TODO: tighten before release"
    ]
  },
  "7-codebase-explore": {
    "question": "Give me a high-level map of the architecture and tell me where retry logic is handled.",
    "keyFacts": [
      "src/lib/http/retryPolicy.ts",
      "export async function withRetry",
      "RETRY_MAX = 5",
      "src/lib/http/httpClient.ts",
      "class HttpClient"
    ]
  },
  "15-multifile-graph": {
    "question": "Walk me through what happens when Checkout.submitOrder runs — specifically how it captures the payment via capturePayment.",
    "keyFacts": [
      "async submitOrder(cart: Cart)",
      "this.payments.capturePayment",
      "async capturePayment(amount: number, token: string)",
      "this.gateway.sendCharge",
      "async sendCharge(req: ChargeRequest)",
      "this.inventory.reserveStock"
    ]
  },
  "17-python-multifile": {
    "question": "Walk me through what happens when Checkout.submit_order runs — specifically how it captures the payment via capture_payment.",
    "keyFacts": [
      "def submit_order(self, cart):",
      "self.payments.capture_payment(cart.total, cart.token)",
      "def capture_payment(self, amount, token):",
      "self.gateway.send_charge(amount=amount, token=token",
      "def send_charge(self, charge):",
      "self.inventory.reserve_stock(cart.lines)"
    ]
  },
  "11-mcp-tools": {
    "question": "Use jira_search_issues to find the open P0 bugs in the PAY project, then use jira_add_comment to comment on each.",
    "keyFacts": [
      "jira_search_issues",
      "jira_add_comment",
      "P0 bugs in the PAY project"
    ]
  },
  "23-mcp-schema": {
    "question": "After the tool schemas are compressed, can I still tell which tool searches issues and which adds a comment, and what the task is?",
    "keyFacts": [
      "jira_search_issues",
      "jira_add_comment",
      "P0 bugs in the PAY project"
    ]
  },
  "24-agent-toolcalls": {
    "question": "Summarize the root cause of the double-charge and the one-line fix.",
    "_note": "Facts live in the final assistant turn, which window_budget pins; the cropped middle is the early investigation.",
    "keyFacts": [
      "re-invokes capture() without forwarding the idempotency key",
      "second authorized capture",
      "src/payments/payments.ts"
    ]
  },
  "12-rag-overfetch": {
    "question": "What is the refund window for annual plans, and is it different for monthly plans?",
    "keyFacts": [
      "billing/refund-policy.md",
      "full refund within 30 days",
      "14 days from the charge date",
      "Monthly subscriptions have a shorter refund window"
    ]
  },
  "13-prompt-boilerplate": {
    "question": "Process the nightly ticket batch and output one line per ticket as \"TCK-<id>: <category>\".",
    "keyFacts": [
      "TCK-4100: I was charged twice",
      "TCK-4102: Please add a dark mode",
      "TCK-4105: App crashes when I open",
      "TCK-4106: How do I change the email",
      "TCK-4103: Cannot log in after resetting"
    ]
  },
  "3-github-triage": {
    "question": "Which of these open issues are P0 authentication bugs that should be fixed this week? List the issue numbers.",
    "keyFacts": [
      "#1300",
      "#1281",
      "#1243",
      "Login fails with 401 after token refresh",
      "SSO (SAML) users can bypass MFA enrollment",
      "priority/P0"
    ]
  },
  "8-long-session": {
    "question": "Given everything across all subsystems, where should the shared retry logic live, and which subsystems should be migrated first?",
    "_note": "The agent consolidates its findings into a short recommendation in the final turns; window_budget pins those recent turns, so the answer (canonical location + migration order) survives the crop. Key facts are that recommendation's content.",
    "keyFacts": [
      "lib/http/retryPolicy",
      "orders first, then billing, then inventory"
    ]
  },
  "16-test-run": {
    "question": "Which tests failed, and what is the root cause of each? Ignore the ones that passed.",
    "_note": "Markers are in command_digest's reformatted output shape (it rewrites, so raw-log strings don't survive verbatim even though the facts do).",
    "keyFacts": [
      "FAIL: test_capture_declined_card_releases_hold",
      "assert 'held' == 'released'",
      "test_annual_refund_window_proration",
      "assert 1666 == 0",
      "only 2 of SKU-1001 left in stock"
    ]
  },
  "18-session-recall": {
    "question": "Catch me up on the payments-retry branch: where does it stand, what changed and why, and what was the next step I had planned?",
    "keyFacts": [
      "branch=payments-retry",
      "reworked capturePayment to be idempotent",
      "built the dead-letter path",
      "park them in payments_dlq",
      "alert when DLQ depth crosses 10"
    ]
  },
  "27-read-service-ts": {
    "question": "How are capture and refund structured, and what idempotency key do they use?",
    "_note": "code_graph keeps declaration signatures and collapses bodies — so the keyfacts are signature lines, which survive.",
    "keyFacts": [
      "export class PaymentService",
      "async capture(amount: number, ctx: TxnContext): Promise<TxnResult>",
      "async refund(amount: number, ctx: TxnContext): Promise<TxnResult>"
    ]
  },
  "28-read-module-py": {
    "question": "How do capture and refund work, and what is the idempotency key format?",
    "_note": "code_graph keeps def signatures and collapses bodies — keyfacts are signature lines.",
    "keyFacts": [
      "class PaymentService:",
      "def capture(self, amount, ctx):",
      "def refund(self, amount, ctx):"
    ]
  },
  "29-orders-json": {
    "question": "Which orders failed, and what failure_code does each failed order show? Ignore the paid ones.",
    "_note": "context_compression minifies the JSON (drops the space after the colon), so keyfacts are bare values, not \"key\": \"value\" pairs.",
    "keyFacts": [
      "ord_100003",
      "ord_100037",
      "card_declined",
      "insufficient_funds"
    ]
  },
  "30-metrics-json": {
    "question": "Which timestamp has p99 over 4000, and how many errors at that point?",
    "_note": "context_compression minifies; keyfacts are the spike values (unique in this series).",
    "keyFacts": [
      "4200",
      "512"
    ]
  },
  "31-long-toolsession": {
    "question": "Summarize the root cause of the dropped transactions and the one-line fix.",
    "_note": "window_budget pins the final turn where the answer lives; the early file-read turns are cropped.",
    "keyFacts": [
      "recon/cursor.ts advances the page cursor before the batch is acknowledged",
      "advance the cursor only after ack"
    ]
  },
  "32-vocab-mismatch-rag": {
    "question": "Folks keep experiencing involuntary disconnections — mid-workflow they get bounced to the authentication prompt. What mechanism forcibly revokes their ongoing credential?",
    "_note": "Vocabulary mismatch: query says 'involuntary disconnections' / 'bounced', answer says 'session eviction' / 'concurrent_login_policy' / 'auth context invalidated'. BM25 < 6 hits, semantic re-rank fires.",
    "keyFacts": [
      "session eviction triggered by concurrent_login_policy",
      "auth context invalidated",
      "IdP SAML certificate rotation",
      "max_concurrent_sessions=1"
    ]
  },
  "33-synonym-gap-logs": {
    "question": "Our monitoring flagged resource exhaustion and capacity saturation on one of the pods. Dig through these application logs and find what happened.",
    "_note": "Synonym gap: query says 'resource exhaustion' / 'capacity saturation', answer says 'OOM killer' / 'heap exhaustion' / 'cgroup limit'. BM25 < 6 hits (1 hit on 'exhaustion'), semantic re-rank fires.",
    "keyFacts": [
      "OOMKilled event",
      "heap exhaustion after 14 GC cycles",
      "container cgroup limit 2Gi breached",
      "checkout-service-7f8b9c"
    ]
  },
  "34-repeat-reads": {
    "question": "What is the exact root cause of the burst starvation in the checkout rate limiter, and which invariant should a regression test pin down?",
    "_note": "The limiter file and git status each appear twice byte-identically; facts anchor on the FIRST copies, which context_dedupe must keep verbatim while collapsing the repeats.",
    "keyFacts": [
      "Math.floor(1000 / cfg.refillRatePerSec)",
      "refillRatePerSec: 10",
      "maxBurst: 40",
      "\"checkout\": { \"rps\": 25",
      "test/middleware/rateLimiter.burst.test.js",
      "{ tokens: 1, updatedAtMs: 40 }"
    ]
  },
  "35-flaky-test-rerun": {
    "question": "Which payments test is actually flaky vs genuinely failing, what changed between the two runs, and which fixture likely causes the instability?",
    "_note": "Run 2 is a near-duplicate of run 1; the flipped 'refund_idempotency PASSED' line is a CHANGED line and must survive inside the unified delta context_dedupe keeps.",
    "keyFacts": [
      "ConnectionResetError: [Errno 104] Connection reset by peer",
      "refund_idempotency PASSED",
      "expected 409, got 200 — replay window not enforced",
      "pool_max_size=4",
      "reruns = 2"
    ]
  },
  "36-stale-observations": {
    "question": "Summarize the root cause of the stuck orders-service v2.31.0 rollout and its current status; what remains before promoting to production?",
    "_note": "The bulky helm/describe/pods dumps are maskable and no keyfact depends on them; every fact survives in the error-bearing log slice (an Anthropic tool_result flagged is_error: true — the only hard-keep observation_mask honors), an assistant turn (never maskable), or a tool result past the keepRecentTurns=3 cutoff.",
    "keyFacts": [
      "failed to bind :8080",
      "CrashLoopBackOff",
      "HTTP_PORT resolved to 8081",
      "inbound listener moved to :15006",
      "successfully rolled out",
      "readyReplicas: 3",
      "\"version\":\"v2.31.0\""
    ]
  },
  "37-durable-blob": {
    "question": "Which flagged dependency blocks commercial redistribution outright as deployed, and what should replace each of the three copyleft packages?",
    "_note": "The 100 KB manifest blob is externalized wholesale behind a durable handle; facts deliberately live OUTSIDE it, in the license-scan output and the assistant summary.",
    "keyFacts": [
      "libfoo-native 2.4.1",
      "pdf-render 1.9.0",
      "geo-tiles 0.8.2",
      "AGPL triggers on network use alone",
      "312 packages"
    ]
  },
  "38-anthropic-context-trim": {
    "question": "Lay out the landscape refactor plan for the invoice PDF generator: what changes in usableHeight and the block builders, and which tests cover the rotated grid?",
    "_note": "provider_context_trim is annotation-only. One fact per tool round, including the earliest — anything below 100% survival is a do-no-harm violation, not a quality trade.",
    "keyFacts": [
      "PAGE = { width: 595, height: 842, margin: 48 }",
      "orientation === 'landscape' ? PAGE.width : PAGE.height",
      "breaks pages between blocks, never inside a row",
      "0 problems (0 errors, 0 warnings)",
      "render.ts         94.1     88.2"
    ]
  },
  "39-reasoning-downshift": {
    "question": "Confirm the array.js migration is complete and behavior-preserving: which lodash calls were replaced with what, and which guarantees were preserved?",
    "_note": "reasoning_budget only touches the reasoning-effort knob; message content is untouched, so 100% survival is the do-no-harm assertion.",
    "keyFacts": [
      "SameValueZero",
      "Array.prototype.flat(Infinity)",
      "Object.groupBy",
      "6 hunks."
    ]
  },
  "40-output-shaping": {
    "question": "Report the ProfileCard to UserCard rename outcome: scope covered, files changed, and anything left to verify.",
    "_note": "The '[anyray:output-guidance]' fact is ABSENT from the raw payload by design — it asserts the advisory actually fired; the other facts assert nothing else moved.",
    "keyFacts": [
      "[anyray:output-guidance]",
      "Applied. 14 files changed.",
      "web/src/features/mentions/HoverPreview.tsx",
      "tsc --noEmit: clean"
    ]
  },
  "42-semantic-rerank-rag": {
    "question": "How do we lower our monthly cloud bill without hurting latency, according to the retrieved internal docs?",
    "_note": "Third vocab-mismatch scenario: the 'Cloud bill anomaly alerts' doc is a lexical trap; the three answer docs (rightsizing, committed-use, egress) share almost no vocabulary with the question.",
    "keyFacts": [
      "rightsizing the c7g fleet saved 31%",
      "p99 latency unchanged at 84ms",
      "committed-use discounts",
      "14,200 USD monthly"
    ]
  }
}
