# Results

All numbers are produced by running the **real Anyray optimizer** over the
synthetic payloads in this repo and measuring the request it returns. Two tiers:

- **Accounting** (the committed `results/*.json`) — whole-request token reduction
  at `chars / 4`, reproducible by anyone with a running optimizer: `./run.sh --all`.
  The **percentage is the number to trust** — it's confirmed against a real tokenizer
  (`tiktoken`) and the optimizer's own estimator. `chars / 4` typically makes the
  absolute token counts a conservative estimate on prose and logs, but can overcount
  on dense JSON or tool-schema content (where real chars-per-token exceeds 4).
- **Live bill** — the provider's real `prompt_tokens`, end-to-end. A real-provider
  cross-check from the Anyray stack is [below](#live-bill-cross-check); a built-in
  `--live-bill` mode is on the [Roadmap](#roadmap).

## Accounting tier — per workload

Whole-request size, optimizer-bypassed (`before`) vs hero-strategy (`after`),
tokens at `chars / 4`. Generated by `./run.sh --all`.

| # | Workload | Suite | Strategy | Knob | Before | After | Saved |
|---|---|---|---|---|--:|--:|--:|
| 1 | Access log (500 requests) — "find the failing requests" | logs-and-data | `relevance_filter` | `keepChars=3000, roles=user` | 26,153 | 1,078 | **96%** |
| 2 | SRE incident — "why did checkout p99 spike at 10:05?" | logs-and-data | `relevance_filter` | `keepChars=12000, roles=user` | 26,385 | 4,326 | **84%** |
| 3 | Synonym-gap logs — "find the resource-exhaustion event" (OOM/cgroup) | logs-and-data | `relevance_filter` | `keepChars=3000, roles=user, semanticRerank=true, semanticWeight=0.7` | 2,963 | 1,185 | **60%** |
| 4 | JSON array (500 items) — "which orders failed and why?" | logs-and-data | `context_compression` | `roles=user` | 74,460 | 16,013 | **78%** |
| 5 | Orders dump (tool result) — "which orders failed and why?" | logs-and-data | `context_compression` | `(defaults)` | 17,940 | 10,436 | **42%** |
| 6 | Metrics series (tool result) — "find the latency spike" | logs-and-data | `context_compression` | `(defaults)` | 12,091 | 7,047 | **42%** |
| 7 | Code search (100 hits) — "where is the retry policy configured?" | code-context | `relevance_filter` | `keepChars=8000, roles=user` | 4,275 | 1,925 | **55%** |
| 8 | Git diff — "any risky change in this PR?" | code-context | `context_compression` | `roles=user` | 5,669 | 3,160 | **44%** |
| 9 | Codebase exploration — "explain the architecture & where retries live" | code-context | `code_graph` | `minChars=200, minBodyLines=2, roles=user` | 4,679 | 3,833 | **18%** |
| 10 | Multi-file trace — "how does Checkout.submitOrder capture payment?" | code-context | `code_graph` | `minChars=200, minBodyLines=2` | 2,092 | 1,446 | **31%** |
| 11 | Multi-file trace (Python) — "how does Checkout.submit_order capture payment?" | code-context | `code_graph` | `minChars=200, minBodyLines=2` | 1,787 | 1,191 | **33%** |
| 12 | Read a large TS service file (tool result) — keep the on-path bodies | code-context | `code_graph` | `minChars=200, minBodyLines=2` | 2,649 | 890 | **66%** |
| 13 | Read a Python module (tool result) — keep the on-path bodies | code-context | `code_graph` | `minChars=200, minBodyLines=2` | 2,218 | 654 | **71%** |
| 14 | MCP tool bloat — 41 tool schemas ride along, 2 are needed | tools-and-rag | `tool_pruning` | `keepUnnamed=true` | 5,614 | 2,076 | **63%** |
| 15 | RAG over-retrieval — top-20 chunks stuffed, 2 hold the answer | tools-and-rag | `relevance_filter` | `keepChars=1200, roles=user` | 1,498 | 487 | **67%** |
| 16 | Vocab-mismatch RAG (20 chunks) — "what revokes their credential?" | tools-and-rag | `relevance_filter` | `keepChars=1500, roles=tool\|function\|user, semanticRerank=true, semanticWeight=0.7` | 2,216 | 643 | **71%** |
| 17 | Templated boilerplate — the same instructions re-pasted 40x | tools-and-rag | `prompt_compression` | `minChars=400` | 5,841 | 914 | **84%** |
| 18 | MCP tool schemas — 41 verbose schemas, compress the prose not the set | tools-and-rag | `tool_schema_compression` | `collapseWhitespace=true, stripBoilerplate=true` | 1,612 | 1,504 | **7%** |
| 19 | Cost-cutting synonyms RAG (14 docs) — "lower the cloud bill" vs "infrastructure spend" | tools-and-rag | `relevance_filter` | `keepChars=1500, roles=tool\|function\|user, semanticRerank=true, semanticWeight=0.85, lexConfidentHits=999` | 942 | 627 | **33%** |
| 20 | GitHub triage — "which open issues are P0 auth bugs?" | agent-ops | `relevance_filter` | `keepChars=2000, roles=user` | 5,583 | 902 | **84%** |
| 21 | Long agent session — keep a 60-message session inside the window | agent-ops | `window_budget` | `maxTokens=8000` | 82,110 | 7,042 | **91%** |
| 22 | Test-suite output — "which tests failed and why?" | agent-ops | `command_digest` | `maxFailures=10, contextLines=12, roles=user` | 1,515 | 351 | **77%** |
| 23 | Agentic tool-call session — fit a multi-step investigation in budget | agent-ops | `window_budget` | `maxTokens=700` | 781 | 587 | **25%** |
| 24 | Long tool-call session — fit a 10-file investigation in budget | agent-ops | `window_budget` | `maxTokens=2500` | 3,509 | 2,207 | **37%** |
| 25 | Repeated reads — the agent re-reads a file and re-runs git status | agent-ops | `context_dedupe` | `(defaults)` | 1,793 | 1,123 | **37%** |
| 26 | Flaky-suite re-run — near-duplicate pytest output collapses to a delta | agent-ops | `context_dedupe` | `(defaults)` | 4,989 | 3,143 | **37%** |
| 27 | Cross-session catch-up — "catch me up on this branch" | memory-recall | `relevance_filter` | `keepChars=2500, roles=tool\|function\|user` | 5,895 | 867 | **85%** |
| 28 | Stale trajectory — mask old bulky observations, keep errors and fresh turns | memory-recall | `observation_mask` | `(defaults)` | 6,722 | 1,100 | **84%** |
| 29 | Durable externalization — a 100 KB manifest becomes a retrieval handle | memory-recall | `output_externalize` | `(defaults)` | 24,581 | 341 | **99%** |
| | **Total (29)** | | | | **338,562** | **77,098** | **77%** |

### Savings by strategy (accounting tier)

Where the headline comes from — so the composition is visible, not hidden behind
one number. The mix is weighted to real coding-agent traffic, so the workhorses
`context_compression`, `window_budget`, and `relevance_filter` carry the suite;
together those top three hold ~80% of the input (33% / 26% / 22%).

| Strategy | Workloads | Before | After | Saved | Share of input |
|---|--:|--:|--:|--:|--:|
| `context_compression` | 4 | 110,160 | 36,656 | **67%** | 33% |
| `window_budget` | 3 | 86,400 | 9,836 | **89%** | 26% |
| `relevance_filter` | 9 | 75,910 | 12,040 | **84%** | 22% |
| `output_externalize` | 1 | 24,581 | 341 | **99%** | 7% |
| `code_graph` | 5 | 13,425 | 8,014 | **40%** | 4% |
| `context_dedupe` | 2 | 6,782 | 4,266 | **37%** | 2% |
| `observation_mask` | 1 | 6,722 | 1,100 | **84%** | 2% |
| `prompt_compression` | 1 | 5,841 | 914 | **84%** | 2% |
| `tool_pruning` | 1 | 5,614 | 2,076 | **63%** | 2% |
| `tool_schema_compression` | 1 | 1,612 | 1,504 | **7%** | <1% |
| `command_digest` | 1 | 1,515 | 351 | **77%** | <1% |

### Guardrails (special accounting)

Reported on their own basis — see [`guardrails/README.md`](guardrails/README.md).

| # | Workload | Strategy | What changed |
|---|---|---|---|
| 1 | Repeated identical request | `semantic_cache` | 2nd identical call hits the cache — provider call avoided (full request saved) |
| 2 | Pasted screenshot | `vision_ocr` | text-bearing image replaced with extracted OCR text (image bytes dropped) |
| 3 | Runaway `max_tokens` | `param_tuning` | output ceiling `max_tokens` 100,000 → 4,096 |
| 4 | Claude prompt-cache prefix | `cache_optimizer` | tools sorted + `cache_control` breakpoints injected on tools+system (cached-read reuse on `claude-*`) |
| 5 | Bloated, over-fetched context | `context_quality` | read-only health score **69/100** (6 bloated, 2 duplicate tool outputs) |
| 6 | Provider context trim (Anthropic) | `provider_context_trim` | native `context_management` edit injected for provider-side tool-result clearing (est. 6,395 input tokens, trigger 4,000) — message bytes unchanged, 100% key-fact survival |
| 7 | Reasoning downshift | `reasoning_budget` | `thinking.budget_tokens` capped 24576 → 8192 on a routine tool-resume turn — the saving is output-side (thinking tokens); live delta lands in `guardrails/results/live-basis.json` |
| 8 | Output shaping | `output_shaping` | one `[anyray:output-guidance]` advisory appended (−7% request bytes by design) — measured live: output tokens 265 → 201 (**−24%**) with judge-PASS answer parity |
| 9 | Mixed-content census | `content_census` | read-only census of the 9 fresh tool outputs (~2,290 tok): json_array 35%, base64 15%, source_code ×2 13%, … — request byte-identical |

## Live-bill cross-check

The accounting tier above measures the request the optimizer returns. The same
strategies, measured **end-to-end through a gateway** against AWS Bedrock (Claude
Sonnet) on the Anyray demo stack, produced these real provider bills — each
workload sent twice (optimizer bypassed vs optimized) and the provider's
`prompt_tokens` compared. (Runs `rmq81lzmk` / `rmq82bng5`, 2026-06-10; the
per-scenario rules were live on the box.)

| Workload | Strategy | Before (tok) | After (tok) | Saved | Answer preserved? |
|---|---|--:|--:|--:|---|
| Access log (500 req) | `relevance_filter` | 47,344 | 3,185 | **93%** | names the 21 error lines |
| SRE incident | `relevance_filter` | 34,146 | 540 | **98%** | identifies the p99 cause |
| GitHub triage | `relevance_filter` | 6,163 | 1,034 | **83%** | lists the P0 auth issues |
| JSON array (500 items) | `context_compression` | 92,747 | 2,495 | **97%** | count + failure codes correct |
| Code search (100 hits) | `relevance_filter` | 3,696 | 1,050 | **72%** | finds the retry config |
| Git diff | `relevance_filter` | 4,206 | 849 | **80%** | flags the loosened admin check |
| MCP tool bloat (41 tools) | `tool_pruning` | 3,253 | 309 | **91%** | still calls the right tool w/ correct args |
| RAG over-retrieval | `relevance_filter` | 1,417 | 472 | **67%** | cites the right refund-policy chunks |
| Templated boilerplate (40×) | `prompt_compression` | 5,151 | 938 | **82%** | all 40 tickets still classify correctly |

The before/after differ from the accounting tier because they are a different
basis — real provider tokenization and the live per-scenario knobs, not the
whole-request `chars / 4` estimate. Both are real measurements of the same
strategies; the accounting tier is the one anyone can reproduce here without
provider keys.

## Does it preserve the answer?

Measured in **[QUALITY.md](QUALITY.md)**: for each workload we define the
answer-bearing key facts and check how many survive the trim. The committed,
reproducible signal is **strict substring survival**: **all 24 preserve the
answer in full** — each workload's strategy is matched to its content and its
settings tuned for strong savings while keeping every answer-bearing fact. A
**committed Claude Opus 4.8 judge**
(each verdict recorded in its `by` field) adds a semantic read and confirms
**31 PASS / 1 MARGINAL / 0 FAIL**. The "Answer preserved?" column above is the demo
stack's end-to-end live validation; the quality benchmark turns it into a
reproducible per-workload score. Anyray's
strategies are also **reversible** (every elided span is retrievable via
`POST /v1/retrieve`) and **fail open** (a slow/down optimizer forwards the request
unoptimized).

## Coverage expansion (added 2026-07-06 · first run 2026-07-08)

Nine workloads were added to close the gap against the optimizer's current
registry — the July "trajectory diet" wave (RFC 0004) shipped seven strategies
this repo had never exercised, several of them **on by default in production**.
The rows below were produced against optimizer **v0.3.41** (monorepo main
`05406e29`, 2026-07-08 — includes the #776 census fresh-boundary change), with
the durable CCR tier and the local MiniLM embedder both available, so nothing
self-gated to a no-op. Accounting rows are folded into the tables above;
guardrail-tier rows are in the guardrails table.

| Workload | Suite | Strategy (new coverage) | Saved | Quality (strict / judge) |
|---|---|---|--:|---|
| 34-repeat-reads | agent-ops | `context_dedupe` (exact duplicates) | 37% | 100% PASS / PASS |
| 35-flaky-test-rerun | agent-ops | `context_dedupe` (near-dupe delta) | 37% | 100% PASS / PASS |
| 36-stale-observations | memory-recall | `observation_mask` | 84% | 100% PASS / PASS |
| 37-durable-blob | memory-recall | `output_externalize` | 99% | 100% PASS / judge 85% MARGINAL¹ |
| 38-anthropic-context-trim | guardrails | `provider_context_trim` | 0% by design² | 100% PASS / PASS |
| 39-reasoning-downshift | guardrails | `reasoning_budget` | 0% by design² | 100% PASS / PASS |
| 40-output-shaping | guardrails | `output_shaping` | −7% by design² | 100% PASS / PASS |
| 41-mixed-content-census | guardrails | `content_census` | read-only | byte-identical (no keyfacts) |
| 42-semantic-rerank-rag | tools-and-rag | `relevance_filter` + `semanticRerank` | 33% | 100% PASS / PASS |

¹ The judge notes the workload's question asks it to *derive* replacement
suggestions that were never in the context (baseline included) — all five key
facts are present; not content loss.
² Guardrail basis: 38 annotates for provider-side clearing without touching
message bytes, 39 saves output-side thinking tokens, 40 spends +7% request bytes
on an advisory that pays back downstream — measured live for 40 at **−24% output
tokens** (265 → 201, judge-PASS answer parity; `guardrails/results/live-basis.json`).
38/39's live deltas (billed input tokens / thinking tokens) rerun via
`run_live.mjs --mode per-strategy` — the first attempt hit the Claude
subscription rate limit.

Two registry entries stay intentionally uncovered: `audited_holdout` (a read-only
holdout-arm marker driven by the top-level holdout config, not the strategy
pipeline — byte-identical by contract) and `code_skeleton` (retired, superseded by
`code_graph` and mutually exclusive with it). Workloads 37 and 42 have environment
prerequisites — see [VALIDATION.md](VALIDATION.md#environment-prerequisites).

## Roadmap

- **Semantic relevance filter.** An embedding-based filter for the vocabulary-
  mismatch workloads where lexical BM25 ranks the answer lines out (see
  [QUALITY.md](QUALITY.md#why-two-arent-green)). *Partially covered now:
  workloads 32/33 carry `semanticRerank: true`, and new workload 42 adds a
  lexical-trap scenario that BM25 alone gets wrong.*
- **`--live-bill` mode in this repo.** Wire the gateway round-trip so the live tier
  is reproducible here, not just on the demo stack.
- **Latency.** Record the hook's added latency per workload (it fails open past
  `ANYRAY_OPTIMIZER_TIMEOUT_MS`, but the typical-case cost is worth publishing).
